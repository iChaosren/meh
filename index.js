//Static Ascii
import coffee from require('./ascii/coffie.ascii');
export const coffee = coffee;

import decoration from require('./ascii/decoration.ascii')
export const decoration = decoration;

import finger from require('./ascii/finger.ascii')
export const finger = finger;

import gimme from require('./ascii/gimme.ascii')
export const gimme = gimme;

import hold_up from require('./ascii/hold_up.ascii')
export const hold_up = hold_up;

import last_supper from require('./ascii/last_supper.ascii')
export const last_supper = last_supper;

import loveyou from require('./ascii/loveyou.ascii')
export const loveyou = loveyou;

import nuke from require('./ascii/nuke.ascii')
export const nuke = nuke;

import oops from require('./ascii/oops.ascii')
export const oops = oops;

import shrug from require('./ascii/shrug.ascii')
export const shrug = shrug;

import sleeping from require('./ascii/sleeping.ascii')
export const sleeping = sleeping;

import table_flip from require('./ascii/table_flip.ascii')
export const table_flip = table_flip;

import troll from require('./ascii/troll.ascii')
export const troll = troll;

import wave from require('./ascii/wave.ascii')
export const wave = wave;

import zoidberg from require('./ascii/zoidberg.ascii')
export const zoidberg = zoidberg;

//Ascii Helpers
export * from './src/decorate';
